<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="Movie Listing"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Hap6binar"
        tools:targetApi="31">
        <activity android:name=".presentation.ui.profile.ProfileActivity" />
        <activity android:name=".presentation.ui.movie_detail.MovieDetailActivity" />
        <activity android:name=".presentation.ui.register.RegisterActivity" />
        <activity android:name=".presentation.ui.login.LoginActivity" />
        <activity android:name=".presentation.ui.home.HomeActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>
